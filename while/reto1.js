/**
 * has una aplicacion que calcule el area 
 * de un circulo (pi*R2). Usa la constante 
 * de la libreria Math
 */
var pi = 3.1415;
var r2 = 0;
var radio = 0;
var i = 0;
while (i =r2) {
    var area = Math.PI * Math.pow(radio,2);
    i++;
    r2 =r2 +pi;
    console.log(area); 
}
